package javaExamples;                                   // organizes this class into a package

public class MethodOnlyExample {                        // defines the class

    public static void sayHello() {                    // basic method only
                                                       // no main() because this class is just storing behavior

        System.out.println("Hello from the method");   // output inside the method
    }
}